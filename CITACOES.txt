Git
git status - verificado estado dos arquivos
git init - inicializa o git na pasta
git add <nomedoarquivo> - inclui o arquivo a ser controlado pelo git
git add . - pega todos os arquivos, pastas e subpastas
git commit -m "v.0.0.1"
git config --global user.email email@example.com - para identificar o usuário do git (apenas uma vezgit config --global user)

clear - limpa a tela

<obrigatório>
[opcional]

Parabéns para você